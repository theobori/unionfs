from enum import StrEnum


class Field(StrEnum):
    STATUS = "status"
    ACTION = "action"
    MESSAGE = "message"
    SOURCE = "source"
    DESTINATION = "destination"
    INSERT_TYPE = "insert_type"
    ROOT = "root"
